# Annotations Setup

Run `supabase/annotations.sql` in the Supabase SQL editor to enable margin notes / talk-back annotations.

## What it adds
- page annotations for weekly pages
- page annotations for daily pages
- user-scoped RLS policies
