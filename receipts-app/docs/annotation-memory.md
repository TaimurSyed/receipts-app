# Annotation Memory

Receipts now uses recent user-written margin notes as soft memory when generating future reflections.

## Purpose
If the user keeps correcting the notebook in a particular direction, future pages should gradually adapt instead of repeating the same wrong read.

## Scope
- recent user annotations only
- used as guidance, not hard truth
- should help tone and interpretation improve over time
