# Voice Playback Setup

Run `supabase/voice-entry-path.sql` in the Supabase SQL editor to add the `audio_path` column used for playback.

## What it enables
- storing the uploaded voice memo path on each entry
- generating signed playback URLs later in the app
